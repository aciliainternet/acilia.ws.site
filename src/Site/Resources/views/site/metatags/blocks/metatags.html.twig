<title>{{ title|striptags }}</title>
<meta name="title" content="{{ title }}" />
<meta name="description" content="{{ description|truncate(158) }}" />
<meta name="keywords" content="{{ keywords }}" />